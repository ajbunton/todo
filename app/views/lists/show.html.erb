<h1 id = "list_name" ><%= @list.name %></h1>


<h2>Incomplete Tasks</h2>
<div id="incomplete_tasks">
	<%= render @incomplete_tasks %>
</div>

<h2>Complete Tasks</h2>
<div id="complete_tasks">
	<%= render	@complete_tasks %>
</div>

<p><%= link_to "Add a new task to this list", new_list_task_path(@list.id), id: "new_task_link", remote: true %></p>

<p><%= link_to "Edit this list's name", edit_list_path(@list), remote: true, id: "edit_list_name" %></p>

<p><%= link_to "Back to All Lists", lists_path %></p>